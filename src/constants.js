const numSphereSegments = 48;
const moonRadius = 1738; // km
const earthRadius = 6378.1366; // km
const sunRadius = 695700; // km
const earthMoonDist = 384000; // km
const earthSunDist = 149597870.700 / 80; // km , 80 is just to bring the sun closer 

export {
    numSphereSegments,
    moonRadius,
    earthRadius,
    sunRadius,
    earthMoonDist,
    earthSunDist
}
